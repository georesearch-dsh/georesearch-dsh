# `@georesearch/dsh-validation-service`

Phase 6 Host-generated ValidationPlans and deterministic ValidationReports for
geodata, experiment results, and citations. Reviewer output is stored only as a
strict ReviewRecord and cannot mutate the validated subject.
