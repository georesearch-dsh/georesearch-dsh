# `@georesearch/dsh-writing-service`

Phase 6 writing surface. A Writing child can read only a committed WritingPacket,
produce a strict ManuscriptCandidate, and invoke a deterministic audit. The audit
requires every prose block to cite eligible Claims, every literature citation to
trace to Evidence, and every numeric token to trace to a ResultRecord.
